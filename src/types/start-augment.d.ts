// Ambient placeholder kept for future TanStack Start type augmentations.
// `server` property on createFileRoute is already supported via THandlers
// generic in @tanstack/router-core (see FileBaseRouteOptions).
export {};
